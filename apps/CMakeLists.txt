FILE(GLOB OSSIM_APP_DIRS "ossim-*")
FILE(GLOB OSSIM_CONFIG_DIR "ossim-config*")
LIST(REMOVE_ITEM OSSIM_APP_DIRS ${OSSIM_CONFIG_DIR})

set(OSSIM_PREFIX "${CMAKE_INSTALL_PREFIX}")
set(OSSIM_LIB_DIR "${CMAKE_INSTALL_PREFIX}/lib")
set(OSSIM_INCLUDES "-I${CMAKE_INSTALL_PREFIX}/include")
set(OSSIM_LIBS "-L${OSSIM_LIB_DIR} -lossim")
set(OSSIM_CFLAGS "")
set(OSSIM_CPPFLAGS "${OSSIM_INCLUDES}")

configure_file(
  ${CMAKE_CURRENT_SOURCE_DIR}/ossim-config.in
  ${CMAKE_CURRENT_BINARY_DIR}/ossim-config
  @ONLY
)

install(PROGRAMS ${CMAKE_CURRENT_BINARY_DIR}/ossim-config DESTINATION bin)

# FIND_PACKAGE(ossim)

# if( OSSIM_FOUND )
#    include_directories( ${OSSIM_INCLUDE_DIR} )
# else( OSSIM_FOUND )
#    message( FATAL_ERROR "Could not find required OSSIM package!" )
# endif( OSSIM_FOUND )
# set(TARGET_COMMON_LIBRARIES ${OSSIM_LIBRARY} ${OPENTHREADS_LIBRARY})

FOREACH(f ${OSSIM_APP_DIRS})
   ADD_SUBDIRECTORY(${f})
ENDFOREACH(f)
